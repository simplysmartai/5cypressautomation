[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --tb=short
    -q
markers =
    integration: marks tests as integration tests (require real API creds)
    slow: marks tests as slow running
filterwarnings =
    ignore::DeprecationWarning
